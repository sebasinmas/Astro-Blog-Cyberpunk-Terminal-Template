---
---

<section class="contact container pixel-corners" id="contact" aria-labelledby="contact-heading">
	<h2 id="contact-heading">CONTACTO<span class="caret-blink" aria-hidden="true"></span></h2>
	<p>
		¿Colaboramos? Escribime para proyectos, consultorías o charlas técnicas. Respondo en breve.
	</p>
	<ul class="channels">
		<li>
			<span class="label" data-flip="SMTP / RFC5322">Email</span>
			<a href="mailto:sebasinmascontacto@gmail.com">sebasinmascontacto@gmail.com</a>
		</li>
		<li>
			<span class="label" data-flip="git://remote">GitHub</span>
			<a href="https://github.com/sebasinmas" target="_blank" rel="noreferrer">github.com/sebasinmas</a>
		</li>
		<li>
			<span class="label" data-flip="LinkedIn Graph API (jk)">LinkedIn</span>
			<a href="https://www.linkedin.com/in/sebastianbeni" target="_blank" rel="noreferrer">Perfil profesional</a>
		</li>
	</ul>
</section>

<style>
	.contact {
		margin-top: 2.5rem;
		margin-bottom: 1rem;
		padding: 1.5rem;
		border: 1px solid color-mix(in srgb, var(--color-primary) 40%, var(--color-outline-variant));
		background: color-mix(in srgb, var(--color-surface) 72%, transparent);
		backdrop-filter: blur(12px);
		border-radius: 0;
		position: relative;
		overflow: hidden;
	}

	.contact::after {
		content: '';
		position: absolute;
		top: 0;
		right: 0;
		width: min(40%, 12rem);
		height: 100%;
		background: linear-gradient(
			270deg,
			rgba(255, 42, 109, 0.06),
			transparent 70%
		);
		pointer-events: none;
	}

	h2 {
		font-size: clamp(1.35rem, 3vw, 1.65rem);
		margin-bottom: 0.5rem;
		color: var(--color-on-surface);
		display: flex;
		align-items: center;
		gap: 0.35rem;
		position: relative;
		z-index: 1;
	}

	.caret-blink {
		display: inline-block;
		width: 0.35rem;
		height: 0.85em;
		background: var(--color-primary);
		box-shadow: 0 0 10px rgba(255, 42, 109, 0.55);
		animation: caret-blink 1s steps(1, end) infinite;
	}

	@keyframes caret-blink {
		50% {
			opacity: 0;
		}
	}

	@media (prefers-reduced-motion: reduce) {
		.caret-blink {
			animation: none;
			opacity: 1;
		}
	}

	p {
		position: relative;
		z-index: 1;
		margin: 0 0 1rem;
		max-width: 42rem;
		color: var(--color-on-surface-variant);
		font-size: 0.95rem;
	}

	.channels {
		position: relative;
		z-index: 1;
		list-style: none;
		margin: 0;
		padding: 0;
		display: grid;
		gap: 0.75rem;
	}

	.channels li {
		display: grid;
		gap: 0.15rem;
	}

	.label {
		font-family: var(--font-mono);
		font-size: 0.65rem;
		text-transform: uppercase;
		letter-spacing: 0.14em;
		color: var(--color-outline);
		position: relative;
		width: fit-content;
		cursor: default;
		transition: color 0.15s ease;
	}

	.label:hover {
		color: var(--color-neon-amber);
		text-shadow: 0 0 8px rgba(255, 176, 0, 0.35);
	}

	.label::after {
		content: attr(data-flip);
		position: absolute;
		left: 0;
		top: 100%;
		margin-top: 0.15rem;
		font-size: 0.58rem;
		letter-spacing: 0.06em;
		text-transform: none;
		color: var(--color-neon-dim);
		opacity: 0;
		transform: translateY(-4px);
		transition:
			opacity 0.18s ease,
			transform 0.18s ease;
		pointer-events: none;
		white-space: nowrap;
		max-width: min(90vw, 18rem);
		overflow: hidden;
		text-overflow: ellipsis;
	}

	.label:hover::after {
		opacity: 1;
		transform: translateY(0);
	}

	.channels a {
		font-family: var(--font-mono);
		font-size: clamp(0.85rem, 2.8vw, 0.92rem);
		min-height: 44px;
		display: inline-flex;
		align-items: center;
		color: var(--color-secondary);
		text-decoration: underline;
		text-decoration-color: color-mix(in srgb, var(--color-secondary) 40%, transparent);
		text-underline-offset: 3px;
		transition:
			color 0.15s ease,
			text-shadow 0.15s ease;
	}

	.channels a:hover {
		color: var(--color-bg);
		background: var(--color-secondary);
		text-decoration: none;
		padding: 0.05rem 0.2rem;
		margin: -0.05rem -0.2rem;
		box-shadow: 0 0 16px rgba(5, 217, 232, 0.45);
		text-shadow: none;
	}
</style>
